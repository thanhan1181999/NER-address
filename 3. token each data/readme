file each_db_of_tab.py sẽ lấy dữ liệu trong thư mục primary data để đánh tag
kết quả tạo ra 6 file gồm:
  obj.txt                 bao gồm obj+feature đã token và đánh tag
  pre.txt                 đã token và đánh tag
  location.txt            bao gồm các địa chỉ dữ liệu trong file csv địa chỉ đã đánh tag
  location_special.txt    đã token và đánh tag
  location_ner.txt        đã token và đánh tag
  location_all.txt        = location.txt + location_special.txt


Chú ý: chỉ các trường sau được tách:
 - số nhà
 - tên đường
 - địa chỉ được đặt tên
 - đặc điểm đối tượng

Các trường sau không cần tách vì chúng cần để nguyên để nhận biết:
  - xã
  - huyện
  - tỉnh
  - đối tượng
  - pre
  - location_special